diff --git a/usr/lib/cups/filter/kyofilter_pre_H b/usr/lib/cups/filter/kyofilter_pre_H
index 09dee24..eb31af3 100755
--- a/usr/lib/cups/filter/kyofilter_pre_H
+++ b/usr/lib/cups/filter/kyofilter_pre_H
@@ -9,7 +9,7 @@ from io import BytesIO
 import tempfile
 import time
 import datetime
-from PyPDF3 import PdfFileWriter, PdfFileReader
+from pypdf import PdfWriter, PdfReader
 import re
 import os
 import ntpath
@@ -247,7 +247,7 @@ def get_base_pdf():
     fileout.write(kyo_in.read())
     fileout.seek(0)
     
-    in_pdf = PdfFileReader(fileout)
+    in_pdf = PdfReader(fileout)
 
     kyo_in.close()
 
@@ -260,7 +260,7 @@ def get_watermark_pdf(watermark_arguments):
     packet = BytesIO()
     draw_watermark(packet, watermark_arguments)
     packet.seek(0)
-    watermark_pdf = PdfFileReader(packet)
+    watermark_pdf = PdfReader(packet)
     return watermark_pdf
 
 def get_base_watermark_position(page_size):
@@ -453,14 +453,14 @@ def draw_repeat_watermark(can, watermark_arguments, page_size):
 
 def apply_watermark_to_pdf(base_pdf, watermark_pdf):
     
-    new_pdf = PdfFileWriter()
+    new_pdf = PdfWriter()
     
-    num_of_pages = base_pdf.getNumPages()
-    watermark = watermark_pdf.getPage(0)
+    num_of_pages = base_pdf.get_num_pages()
+    watermark = watermark_pdf.get_page(0)
     for p in range(num_of_pages):
-        page = base_pdf.getPage(p)
-        page.mergePage(watermark)
-        new_pdf.addPage(page)
+        page = base_pdf.get_page(p)
+        page.merge_page(watermark)
+        new_pdf.add_page(page)
 
     return new_pdf
     
